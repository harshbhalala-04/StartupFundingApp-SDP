import 'package:flutter/material.dart';

class InvestorInvestmentProfileScreen extends StatelessWidget {
  const InvestorInvestmentProfileScreen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}